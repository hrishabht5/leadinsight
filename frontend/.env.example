# LeadPulse Frontend — Environment Variables
# Copy to .env and fill in values

# API base URL — empty means same origin (proxied via Vite in dev)
VITE_API_URL=

# Facebook App ID (for OAuth connect button in Settings)
VITE_FACEBOOK_APP_ID=your_facebook_app_id_here
